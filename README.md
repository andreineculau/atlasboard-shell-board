# atlasboard-shell-board

A shell-board built for Atlasboard (andreineculau/atlasboard fork)

```
$ ls
atlasboard atlasboard-shell-board

$ atlasboard/lib/cli.cli.coffee start --quick --dashboard shell --port 8000
```
